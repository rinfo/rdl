package se.lagrummet.rinfo.main.storage

public class StorageCredentials {
    private boolean admin;
    public StorageCredentials(admin) { this.admin = admin; }
    public boolean isAdmin() { return admin; }
}
