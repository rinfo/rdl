<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

  <modelVersion>4.0.0</modelVersion>

  <groupId>se.lagrummet.rinfo</groupId>
  <artifactId>rinfo-main</artifactId>
  <version>1.0-SNAPSHOT</version>
  <name>RInfo Main</name>
  <url>http://dev.lagrummet.se/rinfo/</url>
  <packaging>war</packaging>

  <parent>
    <groupId>se.lagrummet</groupId>
    <artifactId>rinfo</artifactId>
    <version>1.0-SNAPSHOT</version>
  </parent>

  <dependencies>

    <dependency>
      <groupId>se.lagrummet.rinfo</groupId>
      <artifactId>rinfo-store</artifactId>
    </dependency>

    <dependency>
      <groupId>se.lagrummet.rinfo</groupId>
      <artifactId>rinfo-collector</artifactId>
    </dependency>

    <dependency>
      <groupId>se.lagrummet.rinfo</groupId>
      <artifactId>rinfo-base</artifactId>
    </dependency>

    <dependency>
      <groupId>org.codehaus.groovy.maven.runtime</groupId>
      <artifactId>gmaven-runtime-1.5</artifactId>
    </dependency>

    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
    </dependency>

  </dependencies>

  <build>
    <plugins>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <source>1.5</source>
          <target>1.5</target>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.codehaus.groovy.maven</groupId>
        <artifactId>gmaven-plugin</artifactId>
        <executions>
          <execution>
            <goals>
              <goal>generateStubs</goal>
              <goal>compile</goal>
              <goal>generateTestStubs</goal>
              <goal>testCompile</goal>
            </goals>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>org.mortbay.jetty</groupId>
        <artifactId>maven-jetty-plugin</artifactId>
        <version>6.1.7</version>
        <configuration>
          <scanIntervalSeconds>10</scanIntervalSeconds>
          <contextPath>/</contextPath>
        </configuration>
      </plugin>

      <plugin>
        <artifactId>maven-antrun-plugin</artifactId>
        <executions>
          <execution>
            <phase>generate-resources</phase>
            <goals>
              <goal>run</goal>
            </goals>
            <configuration>
              <tasks>
                <delete file="${project.build.outputDirectory}/rinfo-main.properties"/>
                <copy file="src/environments/${environment}/rinfo-main.properties"
                      tofile="${project.build.outputDirectory}/rinfo-main.properties"/>
              </tasks>
            </configuration>
          </execution>
        </executions>
      </plugin>

    </plugins>
  </build>

</project>
